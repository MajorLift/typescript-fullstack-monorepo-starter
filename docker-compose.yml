version: '3'
services:
  db:
    image: 'postgres:latest'
    ports:
      - 5432:5432
    volumes:
      - ./db-data/:/var/lib/postgresql/data/
    env_file:
      - .env
      - .env.dev
      - .env.test
